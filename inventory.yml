all:
  vars:
    ansible_port: 22
    ansible_ssh_user: ansible
    ansible_ssh_private_key_file: ~/.ssh/ansible

  children:
    cluster_hosts:
      hosts:
        localhost:
          ansible_host: localhost
        delegate:
          ansible_host: localhost
